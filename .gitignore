# Dependencies
node_modules/

# Environment variables — never commit secrets
.env
.env.local

# Build output
dist/

# OS noise
.DS_Store

# Sensitive config files — never commit these
config/resume-baseline.ts
config/discovery.ts